# Dependencias del backend (FastAPI). Instalar con el .venv activado:
#   pip install -r requirements.txt
# Luego ejecutar siempre uvicorn desde el directorio app/ (donde está main.py).
#
# Versiones alineadas con pyproject.toml

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pyyaml>=6.0.2
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
pillow>=11.0.0
qrcode[pil]>=8.0
pywin32>=308; sys_platform == "win32"
